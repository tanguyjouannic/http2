use http2::header::huffman::{Path, Tree};

#[test]
pub fn test_huffman() {
    // let path: Path = Path::from(0xffffea);
    // println!("{:?}", path.directions);
    let tree: Tree = Tree::new().unwrap();
}
